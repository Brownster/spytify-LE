[global]
# Spotify credentials
username = "brownster007"
password = "tcwacf1979"

# Audio backend
backend = "pulseaudio"

# Device configuration
device_name = "YOUR_DEVICE_NAME"
device_type = "computer"

# Audio quality
bitrate = YOUR_BITRATE
volume_normalisation = true

# Cache settings
cache_path = "/tmp/spotifyd"
max_cache_size = 1000000000

# Logging
verbose = true

# No daemon mode (supervisor will manage)
no_daemon = false

# Use system audio
use_mpris = true
dbus_type = "user"
